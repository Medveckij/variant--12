# Використовуємо базовий образ з підтримкою Java
FROM openjdk:11

# Копіюємо файли проекту в контейнер
COPY . /app

# Переміщаємося в робочу директорію проекту
WORKDIR /app

# Компілюємо та будуємо проект
RUN javac Main.java

# Встановлюємо команду для запуску програми
CMD ["java", "Main"]

# Вказуємо автора образу
LABEL maintainer="Your Name"

# Вказуємо порт, який використовує програма (якщо потрібно)
EXPOSE 8080
